<% if line_item == @current_item%>
<tr class="line-item-highlight">
<% else%>
<tr>
<% end %>
  <td class="quantity"><%= line_item.quantity %></td>
  <td><%= line_item.product.title %></td>
  <td id="increment"><%= line_item.quantity %></td>
  <td><%= button_to  "+", increment_line_item_path(line_item), remote: true %></td>
  <td id="decrement"><%= button_to  "-", decrement_line_item_path(line_item), remote: true, class: "decrement" %></td>
  
  <td class="price"><%= number_to_currency(line_item.total_price) %><%= button_to  " ×", line_item, method: :delete,
  data: {confirm: "Are your sure?"} %></td>
</tr>